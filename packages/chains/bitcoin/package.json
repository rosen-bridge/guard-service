{
  "name": "@rosen-chains/bitcoin",
  "version": "8.1.1",
  "description": "this project contains bitcoin chain for Rosen-bridge",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rosen-bridge/rosen-chains.git",
    "directory": "packages/chains/bitcoin"
  },
  "license": "MIT",
  "author": "Rosen Team",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "CHANGELOG.md"
  ],
  "scripts": {
    "build": "tsc --build tsconfig.build.json",
    "clean": "rimraf dist && rimraf tsconfig.build.tsbuildinfo",
    "coverage": "npm run test -- --run --coverage",
    "lint": "eslint --fix . && npm run prettify",
    "lint:check": "eslint . && npm run prettify:check",
    "prettify": "prettier --write . --ignore-path ../../../.gitignore",
    "prettify:check": "prettier --check . --ignore-path ../../../.gitignore",
    "release": "npm run test -- --run && npm run clean && npm run build && npm publish --access public",
    "test": "NODE_OPTIONS='--import tsx' vitest",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@vitest/coverage-istanbul": "^3.1.4",
    "rimraf": "^6.0.1",
    "tsx": "^4.20.5",
    "typescript": "^5.8.3",
    "vitest": "^3.1.4"
  },
  "dependencies": {
    "@rosen-bridge/abstract-logger": "^2.1.0",
    "@rosen-bridge/bitcoin-utxo-selection": "^1.1.3",
    "@rosen-bridge/json-bigint": "^0.2.0",
    "@rosen-bridge/rosen-extractor": "^9.0.0",
    "@rosen-bridge/selection-types": "^0.1.0",
    "@rosen-bridge/tokens": "^3.2.0",
    "@rosen-chains/abstract-chain": "^14.0.1",
    "bitcoinjs-lib": "^6.1.5"
  },
  "engines": {
    "node": ">=22.18.0"
  },
  "keywords": [
    "rosen"
  ]
}
